# Database Configuration (REQUIRED - use strong passwords!)
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
POSTGRES_PORT=
DATABASE_URL=

# Application Configuration
PORT=
NODE_ENV=

# External API Configuration
META_API_BASE_URL=
META_API_TIMEOUT=

# External API Retry Configuration (Optional) ⭐ NUEVA
# Number of retry attempts for failed API requests (default: 3)
META_API_RETRY_ATTEMPTS=

# Sync Operations Configuration (Optional) ⭐ NUEVA
# Timeout for sync/all operation in milliseconds (default: 300000 = 5 minutes)
SYNC_ALL_TIMEOUT=